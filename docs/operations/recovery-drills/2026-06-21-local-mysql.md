# MySQL Restore Drill - 2026-06-21

- Environment: local Docker MySQL 8.4, persistent development data volume.
- Backup: transactional compressed dump generated by `ops/mysql/backup.sh` with SHA-256 sidecar.
- Restore target: isolated temporary database `goalmate_restore_drill_20260621113827`.
- Result: `PASS`.
- Restored tables: 31.
- Completed Prisma migrations in restored database: 24.
- Measured restore and verification duration: 1 second.
- Cleanup verification: zero databases matching `goalmate_restore_drill_%` remained.
- PITR prerequisite verification: `log_bin=ON`, `binlog_format=ROW`.
- Observed local RPO: backup creation time; production RPO depends on external backup and binlog archival schedules.
- Observed local RTO: 1 second for restore/structural verification; production target remains 60 minutes and must include data transfer, application smoke tests and cutover.

The drill validates that the committed scripts produce a checksum-verifiable dump and can restore it without overwriting the source database. It does not substitute for a cloud failure-domain drill or production-scale timing.

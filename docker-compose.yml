services:
  mysql:
    image: mysql:8.4
    command:
      - --log-bin=mysql-bin
      - --binlog-format=ROW
      - --server-id=1
      - --binlog-expire-logs-seconds=604800
    container_name: goalmate-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: goalmate
      MYSQL_USER: goalmate
      MYSQL_PASSWORD: goalmate
    ports:
      - "3307:3306"
    volumes:
      - goalmate_mysql_data:/var/lib/mysql
      - ./ops/mysql/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-ugoalmate", "-pgoalmate"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7
    container_name: goalmate-redis
    ports:
      - "6379:6379"
    volumes:
      - goalmate_redis_data:/data

  clamav:
    image: clamav/clamav:stable
    container_name: goalmate-clamav
    profiles: ["security"]
    ports:
      - "3310:3310"
    volumes:
      - goalmate_clamav_data:/var/lib/clamav
    healthcheck:
      test: ["CMD", "/usr/local/bin/clamdcheck.sh"]
      interval: 15s
      timeout: 10s
      retries: 20

volumes:
  goalmate_mysql_data:
  goalmate_redis_data:
  goalmate_clamav_data:

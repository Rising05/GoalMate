route:
  receiver: operations-webhook
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operations-webhook
    webhook_configs:
      - url: https://replace-with-secret-managed-operations-webhook.example.com/alerts
        send_resolved: true
